<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Guia de Perguntas</title>
    <!-- Insira aqui o link para o CSS do Bootstrap 5 -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
    />
    <style>
      .navbar-brand img {
        height: 60px; /* Aumenta o tamanho do logo */
      }
      .nav-link {
        font-size: 1.1em; /* Aumenta o tamanho da fonte do menu */
        margin-right: 10px; /* Adiciona espaçamento entre os itens do menu */
      }
      .navbar-dark .navbar-nav .nav-link {
        color: rgba(
          255,
          255,
          255,
          0.9
        ); /* Ajusta a cor do texto do menu para uma melhor visibilidade */
      }
      .navbar-dark .navbar-nav .nav-link:hover,
      .navbar-dark .navbar-nav .nav-item.active .nav-link {
        color: rgba(
          255,
          255,
          255,
          1
        ); /* Aumenta a opacidade do texto do menu no hover e no item ativo */
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-info">
      <div class="container">
        <a class="navbar-brand" href="/">
          <img src="/img/guia.png" alt="Logo Guia de Perguntas" />
          Guia de Perguntas
        </a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item active">
              <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/perguntar">Perguntar</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Inserir o resto do corpo da página aqui -->

    <!-- Insira aqui o script JS do Bootstrap 5 -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
